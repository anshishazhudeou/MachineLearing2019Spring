import numpy as np
from sklearn.neighbors import NearestNeighbors

# 10 data block
D = np.empty(shape=[10,1])

# 10 label block
L = np.empty(shape=[10,1])

# initialise blocks

for i in range(10):


print("garbage")
